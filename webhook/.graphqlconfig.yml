projects:
  server:
    schemaPath: src/generated/schema.graphql
    extensions:
      endpoints:
        default: http://localhost:4000
      codegen:
        - generator: graphql-binding
          language: typescript
          input: src/kudos.ts
          output:
            binding: src/generated/kudos.ts
